// Ziply by Alyx Shang.
// Licensed under the FSL v1.

/// Re-exporting the `app` module.
//pub usingnamespace @import("app.zig");

/// Re-exporting the `err` module.
pub usingnamespace @import("err.zig");

/// Re-exporting the `data` module.
pub usingnamespace @import("data.zig");

/// Re-exporting the `utils` module.
pub usingnamespace @import("utils.zig");

/// Re-exporting the `parsed` module.
pub usingnamespace @import("parsed.zig");

/// Re-exporting the `strings` module.
pub usingnamespace @import("strings.zig");
